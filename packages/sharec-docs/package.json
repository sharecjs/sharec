{
  "name": "sharec-docs",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "mdbook build",
    "deploy": "npm run build && gh-pages -d ./book"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
