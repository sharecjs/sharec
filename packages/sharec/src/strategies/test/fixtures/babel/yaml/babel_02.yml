presets:
  - - env
    - modules: false
      targets:
        browsers:
          - '> 1%'
          - last 2 versions
          - not ie > 0
plugins:
  - - syntax-dynamic-import
    - foo: bar
env:
  test:
    presets:
      - stage-2
