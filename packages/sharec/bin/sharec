#!/usr/bin/env node

const minimist = require('minimist')
const sharec = require('../src/sharec')

const argv = minimist(process.argv.slice(2))

try {
  sharec(process.env.PWD, process.env.INIT_CWD, argv)
} catch (err) {
  console.error(err)
  process.exit(1)
}

process.exit(0)
