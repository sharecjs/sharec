#!/usr/bin/env node

const minimist = require('minimist')
const sharec = require('../src/sharec')

const argv = minimist(process.argv.slice(2))

;(async () => {
  await sharec(process.env.PWD, process.env.INIT_CWD, argv)
})()
